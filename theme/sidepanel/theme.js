(function () {
    var body = document.body;
    if (!body) return;
    if (!body.classList.contains('theme-sidepanel')) return;

    window.addEventListener('resize', function () {}, { passive: true });
})();
